# API Base URL for production builds
# In development, Vite proxy is used (see vite.config.ts)
# For production (Vercel → Railway), set this to your Railway backend URL:
# VITE_API_BASE_URL=https://your-app.railway.app
VITE_API_BASE_URL=
