export function UserManagement() {
  return (
    <section className="panel">
      <p className="eyebrow">Operator note</p>
      <h3>User management is intentionally narrow.</h3>
      <p>
        The current backend exposes impersonation, not full admin CRUD. This surface is reserved for the next admin API batch if you want it.
      </p>
    </section>
  );
}
